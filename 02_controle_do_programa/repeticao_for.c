#include <stdio.h>

int main()
{
	int contador;
	
	/* inicialização, condição de repetição e incremento */
	for(contador = 1; contador <= 10; contador++)
	{
		printf("%d\n", contador);
	}
	
	return 0;
}